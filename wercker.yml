box: python

build:
  steps:
    - script:
        name: prepare output
        code: rm -rf $WERCKER_ROOT/.git
    - script:
        name: test python version
        code: python --version

publish:
  steps:
    - internal/publish-step:
      owner: rdcojocaru
